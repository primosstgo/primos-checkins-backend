sudo docker-compose build
// Esta weá a veces da un error pero todo bn
sudo docker-compose run --rm app django-admin startproject core .
sudo docker-compose up
sudo docker exec -it django_container /bin/bash
